import styled from "styled-components";

const TopHeader = styled.header`
  display: flex;
  padding: 25px;
  font-weight: bold;
  font-size: 28px;
  color: #304ffe;
`;

const Nav = () => {
  return (
    <>
      <TopHeader>Randomly</TopHeader>
    </>
  );
};

export default Nav;
